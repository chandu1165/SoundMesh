# Auralyze Flutter App

Flutter product shell for the Auralyze audio diagnosis copilot.

Useful commands:

```cmd
flutter analyze
flutter test
flutter build web
flutter build windows
flutter build apk --debug
dart run tool\analysis_smoke.dart
```

The web build is served by the root `scripts\start_product.cmd` script.
